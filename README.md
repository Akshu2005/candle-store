# CandleShop Full — Django (Auth, Orders, Wishlist, Dummy Payment)

This is an enhanced Django project ready for local testing and Render deployment.

**Sample uploaded image path** used in fixtures and media: `/mnt/data/19167d8c-bf8e-4e27-b0cb-18217ae30b68.png` (already copied into media during prior steps).

See instructions in `RUNNING.md`.
